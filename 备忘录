# 编译项目
mvn clean compile

# 运行测试
mvn clean test

# 打包项目
mvn clean package

# 通过参数禁用Nacos和Seata
java -jar target/user-service-0.0.1-SNAPSHOT.jar --spring.cloud.nacos.discovery.enabled=false --seata.enabled=false
java -jar target/user-service-0.0.1-SNAPSHOT.jar --spring.profiles.active=dev --spring.cloud.nacos.discovery.enabled=false --seata.enabled=false


#释放占用8082端口的进程：
netstat -ano | findstr 8082
taskkill /PID 进程号 /F


这个日志文件搞了两年半没搞出来